{#
  NE PAS ÉDITER LA CONFIG ICI.
  Source de vérité : backend/student/cmd/config.yaml (les ${VAR} sont résolues
  au runtime par le serveur via secrets.env). Aucune adaptation nécessaire :
  cette config ne référence aucun chemin propre au container.
#}
{{ lookup('ansible.builtin.file', playbook_dir ~ '/../../backend/student/cmd/config.yaml') }}
