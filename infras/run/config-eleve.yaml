database:
  host: ${HOST}
  port: 5432
  user: ${POSTGRES_USER}
  password: ${POSTGRES_PASSWORD}
  name: db_rex

mariaDB:
  host:     ${HOST}
  port:     3306
  user:     ${MARIADB_USER}
  password: ${MARIADB_PASSWORD}
  database: cyber_notes_v2

server:
  host: localhost
  port: 3300

jwt:
  secret: ${JWT_SECRET_ELEVE}
  accessTokenExpiresIn: 15m
  refreshTokenExpiresIn: 2160h

ldap:
#url: 	ldap://ldap.mines-ales.fr:389
  url: ldap://${HOST}:3890
  baseDN: dc=ema,dc=fr

age:
  publicKey: ${AGE_PUBLIC_KEY}
