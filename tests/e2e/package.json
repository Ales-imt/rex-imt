{
  "name": "rex-e2e",
  "private": true,
  "description": "Tests E2E Playwright contre la stack locale (make local)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
