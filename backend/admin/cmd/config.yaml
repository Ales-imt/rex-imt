database:
  host: localhost
  port: 5432
  user: ${POSTGRES_USER}
  password: ${POSTGRES_PASSWORD}
  name: db_rex

mariaDB:
  host:     localhost
  port:     3306
  user:     ${MARIADB_USER}
  password: ${MARIADB_PASSWORD}
  database: cyber_notes_v2

server:
  host: localhost
  port: 3333
  certPath : ./x509/mtls_certs

jwt:
  secret: ${JWT_SECRET}
  accessTokenExpiresIn: 15m
  refreshTokenExpiresIn: 168h

ldap:
  url: ldap://ldap.mines-ales.fr:389
  #url: ldap://localhost:3890
  baseDN: dc=ema,dc=fr

ia:
  provider: rack # "ollama" ou "ragarenn" ou "rack"

ollama:
  baseURL: http://localhost:11434
  model: mistral-nemo

ragarenn:
  baseURL: https://ragarenn.eskemm-numerique.fr/sso/ch@t
  apiKey: ${RAGARENN_API_KEY} 
  model: RedHatAI/Llama-3.3-70B-Instruct-FP8-dynamic # ilaas/gemma-4-31b  RedHatAI/Llama-3.3-70B-Instruct-FP8-dynamic

rack:
  baseURL: https://159.31.247.10
  model: mistral-small:latest
  apiKey: ${RACK_API_KEY}
  caCertPath: /home/vjo/projets/rex/rex-imt/infras/x509/mtls_certs/ca.crt