meta {
  name: get postits
  type: http
  seq: 7
}

get {
  url: http://{{host}}/api/v2/postit
  body: none
  auth: bearer
}

headers {
  Cookie: access_token={{access_token}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
}
