version: "2"
sql:
  - engine: "postgresql"
    queries: "pkg/auth/query.sql"
    schema: "../schema.sql"
    gen:
      go:
        package: "auth"
        out: "pkg/auth"
        sql_package: "pgx/v5"
        omit_unused_structs: true

  - engine: "postgresql"
    queries: "pkg/user/query.sql"
    schema: "../schema.sql"
    gen:
      go:
        package: "user"
        out: "pkg/user"
        sql_package: "pgx/v5"
        omit_unused_structs: true

  - engine: "postgresql"
    queries: "pkg/ledger/query.sql"
    schema: "../schema.sql"
    gen:
      go:
        package: "gen"
        out: "pkg/ledger/gen"
        sql_package: "pgx/v5"
        omit_unused_structs: true
        emit_json_tags: true

  - engine: "postgresql"
    queries: "pkg/presencedata/query.sql"
    schema: "../schema.sql"
    gen:
      go:
        package: "gen"
        out: "pkg/presencedata/gen"
        sql_package: "pgx/v5"
        omit_unused_structs: true
        emit_json_tags: true
